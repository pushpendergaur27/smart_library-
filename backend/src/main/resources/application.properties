# Server
server.port=${PORT:8080}

# Supabase PostgreSQL Database (Pooler)
spring.datasource.url=jdbc:postgresql://aws-0-ap-northeast-1.pooler.supabase.com:6543/postgres
spring.datasource.username=postgres.vckjuggbgmsurfbcfoxx
spring.datasource.password=NP4J6jWtmf7TMSLN
spring.datasource.driver-class-name=org.postgresql.Driver

# Connection pool
spring.datasource.hikari.maximum-pool-size=10
spring.datasource.hikari.minimum-idle=2
spring.datasource.hikari.connection-timeout=30000
spring.datasource.hikari.data-source-properties.sslmode=require

# JPA / Hibernate
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=false
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.properties.hibernate.format_sql=true

# JWT
app.jwt.secret=defaultSecretKeyForDevelopmentOnlyChangeInProduction2024!@#$
app.jwt.expiration-ms=86400000

# Borrowing
app.borrowing.period-days=14
app.reservation.pickup-hours=24
app.borrowing.max-limit=5

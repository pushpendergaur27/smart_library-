# Server
server.port=8080

# Supabase PostgreSQL Database
spring.datasource.url=${DB_URL:jdbc:postgresql://aws-0-ap-northeast-1.pooler.supabase.com:6543/postgres?sslmode=require}
spring.datasource.username=${DB_USERNAME:postgres.vckjuggbgmsurfbcfoxx}
spring.datasource.password=${DB_PASSWORD:NP4J6jWtmf7TMSLN}
spring.datasource.driver-class-name=org.postgresql.Driver

# JPA / Hibernate
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=false
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.properties.hibernate.format_sql=true

# JWT
app.jwt.secret=${JWT_SECRET:defaultSecretKeyForDevelopmentOnlyChangeInProduction2024!@#$}
app.jwt.expiration-ms=86400000

# Borrowing
app.borrowing.period-days=14
app.reservation.pickup-hours=24
app.borrowing.max-limit=5
